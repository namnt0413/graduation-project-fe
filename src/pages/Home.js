import Layout from "../layouts/Layout";
import { useState } from "react";

const Home = () => {
  const HomeContent = (
    <>
      Content
    </>
  );

  return <Layout page={HomeContent} />;
};

export default Home;
